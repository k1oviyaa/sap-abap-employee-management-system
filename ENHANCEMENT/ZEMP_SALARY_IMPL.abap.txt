@AbapCatalog.sqlViewName: 'ZEMP_EMP_V1'
@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'CDS View for Employee Management'
@Metadata.ignorePropagatedAnnotations: true
define view ZEMP_EMPLOYEE_CDS
    as select from zemployeeems
{
    //Employee Key Fields
    key emp_id        as EmployeeID,
    emp_name          as EmployeeName,
    department        as Department,
    designation       as Designation,

    //Salary Field
    @Semantics.amount.currencyCode: 'Currency'
    salary            as Salary,
    currency          as Currency,

    //Date Field
    join_date         as JoiningDate
}
